apiVersion: v1
kind: Service
metadata:
  name: dashboard-service
  namespace: dashboard
spec:
  selector:
    app: dashboard
  type: ClusterIP
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
# ---
# apiVersion: v1
# kind: Service
# metadata:
#   name: dashboard-lb
#   namespace: dashboard
# spec:
#   selector:
#     app: dashboard
#   type: LoadBalancer
#   ports:
#     - protocol: TCP
#       port: 80
#       targetPort: 5000