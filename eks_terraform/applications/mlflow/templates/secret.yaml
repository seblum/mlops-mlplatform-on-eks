apiVersion: v1
kind: Secret
metadata:
  name: mlflow-secret
  namespace: mlflow
type: Opaque
stringData:
  RDS_USERNAME: {{ .Values.RDS_USERNAME }}
  RDS_PASSWORD: {{ .Values.RDS_PASSWORD }}
  RDS_HOST: {{ .Values.RDS_HOST }}
  RDS_PORT: "{{ .Values.RDS_PORT }}"
